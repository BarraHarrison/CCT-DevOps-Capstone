apiVersion: v2
name: bookcatalog
description: Helm chart for the Book Catalog API (Django + DRF), with a bundled PostgreSQL deployment for self-contained local Kubernetes demos.
type: application
version: 0.1.0
appVersion: "1.0.0"
